{
  "custom/pacman": {
    "format": "󰮯 {}",
    "interval": 3600,
    "exec": "checkupdates | wc -l",
    "exec-if": "exit 0",
    "on-click": "kitty -e bash -c 'fastfetch && sleep 0.5 && sudo pacman -Syu; exec bash'",
    "signal": 8
  }
}
